// @TODO (maybe) - call this class rather than the auth implementation (in this case, Firebase) directly
export class ToDoAuthClass {
  constructor() {
    //
  }

  getUser() {
    //
  }

  getAccessToken() {
    //
  }
}
