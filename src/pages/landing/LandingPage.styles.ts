import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme) => ({
  container: {
    display: 'block',
    width: '100%',
    position: 'relative',
  },
  grid: {
    paddingTop: '5rem',
    height: '51rem',
    width: '100%',
    flexWrap: 'nowrap',
  },
}));
